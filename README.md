# 🔍 Image Object & Material Analyzer

> Сервис компьютерного зрения на базе **GigaChat API** — определяет объекты на изображениях и анализирует материалы их изготовления.

---

## 🛠️ Стек технологий

| Компонент | Технология |
|-----------|-----------|
| LLM | [GigaChat API](https://developers.sber.ru/portal/products/gigachat) |
| База данных | PostgreSQL |
| Архитектура | Clean Architecture |

---

## Схема базы данных

<img width="956" height="466" alt="image_2026-02-19_18-16-44" src="https://github.com/user-attachments/assets/d7295edd-8f29-44d7-9006-ab80309b19b8" />

---

## ✨ Возможности

- 📸 **Анализ изображений** — автоматически находит все реальные физические объекты на фото
- 🧱 **Определение материалов** — для каждого выбранного объекта определяет материалы изготовления
- 🗄️ **Хранение результатов** — сохраняет данные в PostgreSQL
- 🚀 **Автоматическое создание БД** — база данных создаётся при запуске, миграции не требуются

---

## 🚀 Быстрый старт

### 1. Настройка подключения к БД

Откройте `appsettings.json` и укажите строку подключения к PostgreSQL:

```json
{
  "ConnectionStrings": {
    "Default": "Host=localhost;Port=5432;Database=yourdb;Username=user;Password=password"
  }
}
```

### 2. Настройка GigaChat API

Добавьте токен авторизации в `appsettings.json`:

```json
{
  "GigaChat": {
    "AuthToken": "ваш_токен_здесь"
  }
}
```

### 3. Запуск

```bash
dotnet run
```

> ✅ База данных будет создана автоматически при первом запуске.

---

## 🤖 Промпты

Сервис использует два промпта для работы с GigaChat API:

<details>
<summary><b>Промпт 1 — Анализ изображения на объекты</b></summary>

```
Ты система компьютерного зрения.

Задача:
Найди ВСЕ реальные физические объекты на изображении.

Важно:
- Только реальные предметы
- Не сцена / не фон / не освещение
- Не "комната", "интерьер", "фото"

Правила ответа:
- Только существительные, на английском
- Без пояснений, без markdown
- Только JSON массив

Формат: ["объект1", "объект2", "объект3"]
Если объектов нет — верни [].
```

</details>

<details>
<summary><b>Промпт 2 — Определение материалов объектов</b></summary>

```
Ты эксперт по материалам.

Дан список объектов: {список}

Для КАЖДОГО объекта определи материалы изготовления.

Правила:
- Только материалы, без описаний и комментариев
- На английском, только JSON
- Не добавляй новые объекты

Формат:
{
  "объект": ["материал1", "материал2"]
}

Пример:
{
  "chair": ["wood", "plastic"],
  "cup": ["ceramic"]
}
```

</details>

---

## 🏗️ Архитектура

Проект реализован по принципам **Clean Architecture**:

```
├── Domain          # Сущности и бизнес-логика
├── Application     # Сервисы и интерфейсы
├── Infrastructure  # GigaChat API, PostgreSQL
└── Presentation    # Точка входа / API
```

> **Примечание:** CQRS намеренно не использовался — для данного проекта это было бы избыточным усложнением.

---

## 📝 Заметки разработчика

- Промпты реализованы как статические строки (без загрузки из внешних файлов) — при наличии более двух промптов рекомендуется вынести их в отдельные файлы конфигурации
- Репозитории присутствуют в проекте, хотя для такого масштаба их использование также можно считать спорным решением
